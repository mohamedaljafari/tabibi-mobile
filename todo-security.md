# إصلاحات أمنية — طبيبي (المريض + الشريك)

## تطبيق المريض (tabibi-mobile)
- [ ] إزالة مفتاح Supabase anon الاحتياطي من lib/supabase.ts (fallback static)
- [ ] تشفير PINs لوحة التحكم (admin-pins.ts) بـ SHA-256 + salt بدل النص الصريح
- [ ] تقوية مولّد توكنات الجلسات (السقوط إلى Math.random في الويب)
- [ ] قفل الحساب بعد محاولات تسجيل دخول فاشلة متكررة (مريض + شريك)
- [ ] ترقية تجزئة كلمات المرور إلى PBKDF2 (تطبيق المريض + الشريك)
- [x] ~~RLS على Supabase~~ — ملغي: مشروع Supabase الخارجي توقف (NXDOMAIN)، الاستبدال أدناه
- [ ] ~~نقل تخزين تطبيق الشريك من Supabase المتوقف إلى قاعدة البيانات الأساسية~~ — نقل التخزين إلى قاعدة البيانات الأساسية (خيار المستخدم: الأفضل والأسهل)

## تطبيق الشريك (tabibi-partner)
- [ ] إزالة مفتاح Supabase anon الاحتياطي من lib/supabase.ts
- [ ] تقوية مولّد توكنات الجلسات (Math.random fallback)
- [ ] قفل الحساب بعد محاولات فاشلة (provider-auth)
- [ ] ترقية hashPasswordStrong/verifyProviderPassword إلى PBKDF2
- [ ] نقل بيانات الشريك إلى قاعدة البيانات الأساسية + طبقة خادم وسيط (tRPC) للمصادقة والعمليات الحساسة

## قرار النقل (20 أغسطس)
المستخدم اختار "الأفضل والأسهل" = الخيار (ب): قاعدة البيانات الأساسية للمشروع (MySQL، Wd5GbXRebFWa7E45nFiQGE)
- Supabase الخارجي fcjfkvlptuxylejmjbkh.supabase.co غير موجود (NXDOMAIN) — توقف/حُذف نهائيًا
- خطة الشريك: جداول tabibi_users/tabibi_sessions على MySQL عبر drizzle + خوادم tRPC في مشروع الشريك (أو مشاركة مشروع المريض) + اختبارات تعاد للوضع المحلي

## التحقق
- [ ] اختبارات المريض 157/157
- [ ] اختبارات الشريك 31/31
- [ ] TypeScript نظيف
- [ ] مراجعة كمبرمج
- [ ] مراجعة كهكر
- [ ] رفع إلى GitHub
